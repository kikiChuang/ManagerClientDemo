using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using Newtonsoft.Json.Linq;
using System.Text;
using System.Threading.Tasks;
using ManagerClientDemo.Entities;
using Newtonsoft.Json;
using Newtonsoft.Json.Serialization;

namespace ManagerClientDemo
{
    class Program
    {
        
        static void Main(string[] args)
        {
            //LogInAdmin();
            //AddAdministrator();
            //AlterPassword();

            //AddScenic();
            //AlterScenic();

            //EditState();
            //GetLostContacts(DateTime.Now.AddDays(-7),DateTime.Now);

            //GetServerLogs(DateTime.Now.AddDays(-7), DateTime.Now);
            //DeleteServerLogs();

            //GetEmergencyCalls(DateTime.Now.AddDays(-7), DateTime.Now);


            int i = 0;
        }
    }
}
